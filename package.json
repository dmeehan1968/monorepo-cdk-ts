{
  "name": "monorepo-cdk-ts",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "app:deploy": "cd packages/app; npm run deploy",
    "app:synth": "cd packages/app; npm run synth",
    "test": "NODE_OPTIONS=\"$NODE_OPTIONS --experimental-vm-modules --no-warnings\" npx jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "22.5.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
